# TEST_TASK

Тестовое задание: мини-модуль "Список задач с просмотром деталей"

## ✨ Функционал

### Основной
| Функция | Описание |
|---------|----------|
| 📥 **Загрузка задач** | Получение данных с JSONPlaceholder API (первые 10 задач) |
| 📋 **Список задач** | Отображение задач в виде карточек с названием и статусом |
| 🔘 **Кнопка статуса** | Изменение статуса задачи без открытия панели (круглая кнопка слева) |
| 🔓 **Кнопка "Открыть"** | Открытие боковой панели с деталями задачи |
| 📌 **Боковая панель** | Детальный просмотр задачи с названием, описанием и статусом |
| 🔄 **Переключение статуса** | Возможность изменить статус прямо в панели |
| ❌ **Кнопка "Закрыть"** | Закрытие панели и возврат к списку |
| 🔍 **Фильтрация задач** | Фильтр "Все / Активные / Выполненные" |
| 📊 **Статистика** | Счетчики общего количества, выполненных и активных задач |
| 🔒 **Блокировка скролла** | Запрет прокрутки фона при открытой панели |

## 📁 Структура проекта
```bash
task-list-app/
├── public/
│ ├── index.html
│ └── ...
├── src/
│ ├── components/
│ │ ├── FilterBar/
│ │ │ ├── FilterBar.tsx
│ │ │ └── FilterBar.module.css
│ │ ├── TaskCard/
│ │ │ ├── TaskCard.tsx
│ │ │ ├── TaskCard.module.css
│ │ │ └── TaskDetails.tsx
│ │ ├── TaskList/
│ │ │ ├── TaskList.tsx
│ │ │ └── TaskList.module.css
│ │ ├── TaskSidebar/
│ │ │ ├── TaskSidebar.tsx # Боковая панель
│ │ │ └── TaskSidebar.module.css # Стили панели
│ │ └── UI/
│ │ ├── Button/
│ │ ├── Loader/
│ │ └── ErrorMessage/
│ ├── hooks/
│ │ └── useTasks.ts # Логика приложения
│ ├── services/
│ │ └── api.ts # Работа с API
│ ├── types/
│ │ └── index.ts # TypeScript типы
│ ├── App.tsx
│ ├── App.module.css
│ └── index.tsx
├── package.json
├── tsconfig.json
└── README.md
```

## 🚀 Установка и запуск

#### 1. Клонировать репозиторий
```bash
git clone https://github.com/ваш-username/task-list-app.git
cd task-list-app
```

#### 2. Установить зависимости
```bash
npm install
```

#### 3. Запустить в режиме разработки
```bash
npm run start
```

#### 4. Открыть в браузере
```bash
http://localhost:3000
```

## 🎨 Принятые решения

### 1. Архитектура компонентов
**Решение**: Разделение на маленькие переиспользуемые компоненты.

**Почему**:
- Каждый компонент решает одну задачу
- Упрощает тестирование и отладку
- Легко поддерживать и расширять
- Компоненты можно переиспользовать

---

### 2. Кастомный хук useTasks
**Решение**: Вся логика работы с задачами вынесена в отдельный хук.

```tsx
export const useTasks = () => {
  const [tasks, setTasks] = useState([]);
  const [selectedTask, setSelectedTask] = useState(null);
  // ... логика загрузки, фильтрации, открытия
}
```
**Почему**:
- Отделение логики от представления
- Легко тестировать
- Можно переиспользовать в разных компонентах
- Код становится чище и понятнее

---

### 3. TypeScript
**Решение**: Полная типизация всех компонентов и данных.

```tsx
interface Task {
  id: number;
  title: string;
  body?: string;
  completed: boolean;
}
```

**Почему**:
- Предотвращает ошибки на этапе разработки
- Улучшает автодополнение в IDE
- Служит документацией
- Облегчает рефакторинг

---

### 4. CSS Modules
**Решение**: Изоляция стилей для каждого компонента.

```css
.card {
  background: white;
  border-radius: 16px;
}
```

**Почему**:
- Стили не конфликтуют между компонентами
- Классы автоматически уникальны
- Легко поддерживать
- Предсказуемое поведение
---

### 5. Боковая панель вместо модального окна
**Решение**: Детали задачи открываются в боковой панели справа.

**Почему**:
- Не перекрывает весь контент
- Сохраняет контекст (список задач виден)
- Современный UX-паттерн
- Лучше адаптируется для мобильных
- Плавная анимация появления
---

### 6. Анимация через CSS transitions
**Решение**: Использование CSS для плавных переходов.

```css
.sidebar {
  right: -600px;
  transition: right 0.4s cubic-bezier(0.4, 0, 0.2, 1);
}
```

**Почему**:
- Работает на GPU (производительно)
- Меньше кода чем JavaScript-анимации
- Плавно на мобильных устройствах
- Просто поддерживать
---

### 7. Обработка состояний
**Решение**: Явная обработка всех состояний приложения.

```tsx
{loading && <Loader />}
{error && <ErrorMessage onRetry={retry} />}
{!loading && !error && <TaskList tasks={filteredTasks} />}
```

**Почему**:
- Пользователь всегда понимает что происходит
---

### 8. Обработка состояний
**Решение**: Добавлена фильтрация по статусу задач.

```tsx
const filteredTasks = tasks.filter(task => {
  if (filter === 'completed') return task.completed;
  if (filter === 'active') return !task.completed;
  return true;
});
```

**Почему**:
- Улучшает навигацию по задачам
- Простая реализация
- Показывает понимание работы с состоянием
---
